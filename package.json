{
  "name": "vehicle-service-booking-system",
  "version": "1.0.0",
  "description": "Vehicle Service Booking System with frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "concurrently \"npm run build --prefix frontend && npm run preview --prefix frontend\" \"npm start --prefix backend\"",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}